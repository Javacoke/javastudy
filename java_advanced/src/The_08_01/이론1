1. 문자열(String) // 공식 덕스를 보면서 학습하였습니다.
클래스와 같은 이름을 가진 메소드는 생성자라고 한다.(ex/String)
String은 문자열을 다루는 클래스이다.

문자열(하나의 이상의 문자들의 집합)자바에서는""로 표기한다.
자바: 문자열 데이터를 저장하기 위해  String 타입을 사용한다.
어떻게 다루느냐에 따라 어플리케이션의 성능이 차이가 난다.

메모리
statck 영역이 아닌 객체와 같이 힙에서 문자열 데이터가 생성되고 다뤄진다.

String 클래스의 특징
기본적으로 자바에서는 String 객체의 값은 변경할 수 없다.
수정불가의 성질 수정하고싶으면 완전 쌔삥으로 만든다.

hashCode() 메소드는 객체의 메모리나 번지를 이용해서 해시코드를 만들어 리턴하는 메소드다

정처기 필기-> os-> 혼공단 컴퓨터 구조 운영체제// 노베이스사람들도 해도 되는지

자바의 String은 왜 Immutable